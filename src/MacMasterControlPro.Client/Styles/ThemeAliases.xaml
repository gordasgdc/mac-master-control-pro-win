<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
        Regula 37.
        Culorile înlocuite aici erau o paletă închisă scrisă de mână
        (#14161A panou lateral, #1E2128 card, #EDEFF2 text, #2B2F36 bordură).
        Pe tema Light ar fi rămas exact la fel — panou negru și text aproape
        alb pe fereastră deschisă. Acum fiecare își ia culoarea din tema
        activă, deci se inversează corect la comutare.
    -->

    <SolidColorBrush x:Key="TextPrimaryBrush" Color="{DynamicResource TextFillColorPrimary}" />
    <SolidColorBrush x:Key="TextSecondaryBrush" Color="{DynamicResource TextFillColorSecondary}" />
    <SolidColorBrush x:Key="WindowBackgroundBrush" Color="{DynamicResource ApplicationBackgroundColor}" />
    <!-- Panoul lateral: un strat mai „adânc" decât fereastra. -->
    <SolidColorBrush x:Key="SidebarBackgroundBrush" Color="{DynamicResource SolidBackgroundFillColorBase}" />
    <SolidColorBrush x:Key="CardBackgroundBrush" Color="{DynamicResource CardBackgroundFillColorDefault}" />
    <SolidColorBrush x:Key="CardStrokeBrush" Color="{DynamicResource CardStrokeColorDefault}" />

    <SolidColorBrush x:Key="TextDisabledBrush" Color="#8E8E93" />

    <!-- Stări: același înțeles în ambele teme, definite o singură dată. -->
    <SolidColorBrush x:Key="StatusWarningBrush" Color="#F0A030" />
    <SolidColorBrush x:Key="LinkAccentBrush" Color="#4EA1F5" />
    <SolidColorBrush x:Key="StatusErrorBrush" Color="#E5484D" />

    <!-- Consola de log: fundal aproape negru cu text verde, convenția
         terminalului. Rămâne la fel în ambele teme — e o suprafață de tip
         terminal, nu o zonă obișnuită de interfață. -->
    <SolidColorBrush x:Key="TerminalSurfaceBrush" Color="#0D0F12" />
    <SolidColorBrush x:Key="TerminalTextBrush" Color="#8FE388" />

    <Style TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="{DynamicResource TextDisabledBrush}" />
                <Setter Property="Opacity" Value="1" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
        <Setter Property="CaretBrush" Value="{DynamicResource TextPrimaryBrush}" />
        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="{DynamicResource TextDisabledBrush}" />
                <Setter Property="Opacity" Value="1" />
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>
