<UserControl x:Class="MacMasterControlPro.Client.Pages.UninstallerPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:controls="clr-namespace:MacMasterControlPro.Client.Controls">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="380" />
            <ColumnDefinition Width="16" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0">
            <Grid Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="🗑️ Dezinstalator" FontSize="20" FontWeight="Bold" VerticalAlignment="Center" />
                <ui:Button Grid.Column="1" x:Name="BulkDeleteButton" Content="Dezinstalează selectate"
                           Appearance="Danger" Visibility="Collapsed" Click="OnBulkDeleteClicked"
                           ToolTip="Șterge complet toate aplicațiile bifate mai jos — bundle-ul + toate urmele lor, într-un singur pas." />
            </Grid>
            <ui:TextBox x:Name="SearchBox" PlaceholderText="Caută aplicație…" Margin="0,0,0,8" TextChanged="OnSearchChanged" />
            <ScrollViewer MaxHeight="560" VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="AppsPanel" />
            </ScrollViewer>
        </StackPanel>

        <StackPanel Grid.Column="2" x:Name="DetailsPanel">
            <TextBlock Text="Alege o aplicație din listă." Foreground="Gray" />
        </StackPanel>
    </Grid>
</UserControl>
